# Default configurations for training and validation
---
CountNet:
  depth: 2
  in_channels: 3
  out_channels: 1

Trainer:
  optimizer:
  loss_metric:

  training:
    epochs: 1

  validation:
    metrics: